# Example environment variables for Docker deployment

# Your domain name
HOST=squoosh.example.com

# Container internal port (always 80)
PORT=80

# External port for standalone deployment
EXTERNAL_PORT=8080

# Image tag (arm64 or build your own for other architectures)
IMAGE_TAG=latest-arm64
